import { copyFixtureIntoTempDir } from "jest-fixtures";

describe.skip("git", () => {
  describe("getCommitThatAddsFile", () => {
    it("should have tests", () => false);
  });
  describe("getChangedFilesSince", () => {
    it("should have tests", () => false);
  });
  describe("add", () => {
    it("should have tests", () => false);
  });
  describe("commit", () => {
    it("should have tests", () => false);
  });
  describe("tag", () => {
    it("should have tests", () => false);
  });
  describe("getChangedPackagesSinceMaster", () => {
    it("should have tests", () => false);
  });
  describe("getChangedChangesetFilesSinceMaste", () => {
    it("should have tests", () => false);
  });

  it("should have tests", async () => {
    // const cwd = await copyFixtureIntoTempDir(__dirname, "with-git");
  });
});
