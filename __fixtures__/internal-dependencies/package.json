{
  "private": true,
  "name": "simple-project",
  "description": "simple project, just two workspaces, no dependencies",
  "version": "1.0.0",
  "bolt": {
    "workspaces": [
      "packages/*"
    ]
  }
}
