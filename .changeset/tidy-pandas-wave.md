---
"@changesets/cli": major
"@changesets/config": major
---

Private packages are no longer versioned by default. Set `privatePackages` or `privatePackages.version` to `true` to opt into versioning them.
