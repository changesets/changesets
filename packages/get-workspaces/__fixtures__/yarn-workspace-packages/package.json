{
  "private": true,
  "name": "yarn-workspace-package",
  "description": "Yarn workspace using the nested package field",
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
