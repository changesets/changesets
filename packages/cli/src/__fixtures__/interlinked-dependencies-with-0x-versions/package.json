{
  "private": true,
  "name": "simple-project",
  "description": "simple project, where packages are all 0.x versions",
  "version": "1.0.0",
  "bolt": {
    "workspaces": [
      "packages/*"
    ]
  }
}
